<!--pages/CheckOutPage/CheckOutPage.wxml-->
<view class="showbutton">
  <van-button plain type="primary" size="large" bindtap="scanCode">{{scanCode.text}}</van-button>
</view>
<view class="errMsg" wx:if="{{!scanCodeValidity}}">{{scanCode.notValidMsg}}</view>